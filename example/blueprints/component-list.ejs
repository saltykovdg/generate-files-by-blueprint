import * as <%= helpers.pascalCase(name) %>Path from './../paths/<%= helpers.pascalCase(name) %>Path';

class <%= helpers.pascalCase(name) %>List extends Component {
  render() {
    return (
      <div>YourListComponent</div>
    );
  }
}

export default <%= helpers.pascalCase(name) %>List;
